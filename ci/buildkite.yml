steps:
  - command: "ci/build.sh"
    label: "build [public]"
    plugins:
      docker#v1.1.1:
        image: "blang/latex:ubuntu"
  - command: "ci/release.sh"
    label: "release [public]"
    plugins:
      docker#v1.1.1:
        image: "blang/latex:ubuntu"
        environment:
          - BUILDKITE_TAG
          - BUILDKITE
          - GITHUB_TOKEN
